import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logo: {
    height: 200,
    width: 200,
    marginTop: -30,
    marginBottom: -25,
  },
  message: {
    marginTop: 4,
  },
  commonError: {
    marginTop: 4,
    marginBottom: -20,
  },
  inputContainer: {
    marginTop: 35,
    marginBottom: 30,
  },
  forgotAccount: {
    marginTop: 20,
  },
  links: {
    alignItems: 'center',
    marginTop: 20,
  },
});
